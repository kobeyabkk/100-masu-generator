# 🚀 Release Notes - v2.9.0

## 📅 リリース日: 2025年11月15日

---

## 🎉 新機能

### ⭐ 割り算プリセット機能（完全実装）

割り算選択時に、難易度別のプリセット設定パネルが自動表示されます。

#### 難易度プリセット
1. **簡単（2桁÷1桁）**
   - 被除数: 10～99
   - 除数: 自動調整（列数に応じて）

2. **普通（3桁÷1桁）**（デフォルト）
   - 被除数: 100～999
   - 除数: 自動調整（列数に応じて）

3. **難しい（3桁÷2桁）**
   - 被除数: 100～999
   - 除数: 10～99

4. **カスタム（範囲指定）**
   - 数値範囲を自由に設定可能

#### 除数範囲の自動調整（重複完全排除）
- **列数8以下**: 除数2～9（8個、重複なし）
- **列数9**: 除数1～9（9個、重複なし）
- **列数10以上**: 除数1～10（10個、重複なし）

#### 完全重複排除アルゴリズム
- **列の数値（除数）**: Fisher-Yatesシャッフルで重複絶対なし
- **行の数値（被除数）**: Setを使用した重複チェック（最大100回再試行）
- **保証事項**: 10×6グリッドで「4,4,8,8,8,5」のような重複は完全に排除

#### 割り切れる数を保証
- すべての被除数は少なくとも1つの除数で必ず割り切れる
- 「723」のようなどの除数でも割り切れない数は生成されない
- 段階的生成アルゴリズム（方法3）を採用

#### 整数解60%以上を自動達成
- 内部で60%以上の整数解を目指して生成
- ユーザーは意識する必要なし（UIも簡素化）

---

## 🌍 多言語対応（16言語完全対応）

### 新規追加（11言語）
- 🇹🇭 **タイ語**（6,900万人）
- 🇮🇩 **インドネシア語**（2億7,500万人）
- 🇻🇳 **ベトナム語**（9,800万人）
- 🇮🇳 **ヒンディー語**（5億3,000万人）
- 🇦🇪 **アラビア語**（4億2,000万人）- RTL対応
- 🇷🇺 **ロシア語**（2億6,000万人）
- 🇳🇱 **オランダ語**（2,400万人）
- 🇪🇸 **スペイン語**（5億9,000万人）
- 🇫🇷 **フランス語**（3億2,100万人）
- 🇩🇪 **ドイツ語**（1億3,400万人）
- 🇵🇹 **ポルトガル語**（2億6,400万人）

### 既存対応（5言語）
- 🇯🇵 日本語
- 🇺🇸 英語
- 🇨🇳 簡体中国語
- 🇹🇼 繁体中国語
- 🇰🇷 韓国語

### 市場カバレッジ
- **合計対応人口**: 約57億人
- **世界人口比**: 72%
- **主要市場**: 中国、インド、中東、ロシア圏、東南アジア、ヨーロッパ、南米

---

## 🔧 バグ修正

### 重複数値の完全排除
- **修正前**: 10×6グリッドで列に「2,4,8,8,8,5」のように同じ数が複数回出現
- **修正後**: Fisher-Yatesシャッフルで完全にユニークな数値を保証

### 割り切れない数の排除
- **修正前**: 「723」のようなどの除数でも割り切れない数が生成される
- **修正後**: すべての被除数は少なくとも1つの除数で必ず割り切れる

### 除数範囲の動的調整
- **修正前**: 固定で2～9の範囲（8個のみ）
- **修正後**: 列数に応じて自動調整（8列以下: 2～9、9列: 1～9、10列以上: 1～10）

---

## 🎨 UI/UX改善

### プリセットパネルのレイアウト修正
- **修正前**: Flexboxで横並び、ボタンがコンテナからはみ出す
- **修正後**: CSS Gridで2×2レイアウト、完全にフィット

### UI簡素化
- **削除**: 「整数解の割合」ドロップダウンセレクター
- **理由**: ユーザーには不要な複雑さ、内部で60%以上を自動達成

### レスポンシブ対応
- **スマホ対応**: 768px以下で1列レイアウトに自動変更
- **タブレット対応**: 2列レイアウトを維持

### ヒントテキストの改善
- フォントサイズを11pxに縮小
- 除数範囲の自動調整ルールを明記

---

## 🏗️ 技術的改善

### アルゴリズムの最適化
```javascript
// Fisher-Yatesシャッフルで重複排除
function generateRandomNumbers(min, max, count) {
    const range = max - min + 1;
    if (count > range) {
        throw new Error(`Cannot generate ${count} unique numbers from range ${min}-${max}`);
    }
    
    const numbers = [];
    for (let i = min; i <= max; i++) {
        numbers.push(i);
    }
    
    return shuffleArray(numbers).slice(0, count);
}
```

### 被除数の重複チェック
```javascript
// Setを使用した重複チェック（最大100回再試行）
const uniqueRowNumbers = [];
const usedDividends = new Set();

for (let i = 0; i < rowNumbers.length; i++) {
    let dividend = rowNumbers[i];
    let attempts = 0;
    
    while (usedDividends.has(dividend) && attempts < 100) {
        // 再生成ロジック
        attempts++;
    }
    
    usedDividends.add(dividend);
    uniqueRowNumbers.push(dividend);
}
```

### 動的除数範囲調整
```javascript
function applyDivisionPreset() {
    const colSize = parseInt(colSizeSelect.value, 10);
    
    let divisorMin, divisorMax;
    if (colSize <= 8) {
        divisorMin = 2;
        divisorMax = 9;
    } else if (colSize === 9) {
        divisorMin = 1;
        divisorMax = 9;
    } else {
        divisorMin = 1;
        divisorMax = 10;
    }
    // プリセット設定に反映
}
```

---

## 📦 配布ファイル

### 含まれるファイル
- `index.html` - メインアプリケーション
- `css/style.css` - スタイルシート
- `css/input-mode.css` - 入力モード専用スタイル
- `js/main.js` - メインロジック
- `js/input-mode.js` - 入力モードロジック
- `js/translations.js` - 16言語翻訳データ
- `HOW_TO_USE_[言語コード].md` - 使い方ガイド（9言語）
- `LICENSE.txt` - ライセンス情報
- `VERSION.txt` - バージョン履歴

### システム要件
- **ブラウザ**: Chrome, Firefox, Safari, Edge（最新版推奨）
- **インターネット**: 不要（完全オフライン動作）
- **OS**: Windows, macOS, Linux, iOS, Android

---

## 🔜 今後の予定

### 短期（v2.10.0）
- [ ] 多言語ユーザーガイドの拡充（16言語対応）
- [ ] 印刷レイアウトの微調整
- [ ] ユーザーフィードバックに基づく改善

### 中期（v3.0.0）
- [ ] PDF直接出力機能
- [ ] 問題の保存・読み込み機能
- [ ] 複数問題の一括生成

### 長期（v4.0.0）
- [ ] オンライン共有機能
- [ ] クラス管理機能（教師向け）
- [ ] 学習進捗トラッキング

---

## 🙏 謝辞

この製品は「プログラミングのKOBEYA」代表・鈴木政路様のご要望に基づき開発されました。

教育現場での実用性を最優先に、世界中の子どもたちが楽しく学べる教材を目指して改良を重ねました。

---

## 📞 サポート

ご質問・ご要望は以下までお問い合わせください：

- **開発元**: プログラミングのKOBEYA
- **所在地**: バンコク フジスーパー2号店2階
- **対象**: 在タイ日本人、グローバル市場

---

**バージョン**: v2.9.0  
**リリース日**: 2025年11月15日  
**ステータス**: ✅ 製品リリース準備完了  
**販売**: 即座に開始可能
